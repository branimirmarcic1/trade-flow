services:
  ratedb:
    image: postgres

  # rate.api:
  #   image: ${DOCKER_REGISTRY-}rateapi
  #   build:
  #     context: .
  #     dockerfile: Services/Rate/Rate.API/Dockerfile

volumes:
  postgres_rate: